# 開発・設計規約（FF14 Craft Market Checker）

本プロジェクトはプロダクション品質（高パフォーマンス・堅牢性・保守性）を維持して運用されています。  
新機能の追加・バグ修正・リファクタリングを実施する際は、以下の設計原則および禁止事項を厳格に遵守してください。

---

## 1. フロントエンド：アルゴリズムと計算量（Performance & Algorithms）

- **O(1) 定数時間参照の徹底**:
  - `marketData` やアイテム一覧に対する配列走査（`.find()`、`.filter()`、`.includes()`）のネストを禁止します。
  - ルックアップには必ず `Map`、`Set`、または `WeakMap` を使用し、事前にインデックス化してください。
- **再帰ツリー計算のメモ化**:
  - レシピツリー展開（`resolveFullTreeForScope` 等）において、同一アイテム・同一条件のサブツリーを無駄に再計算させないでください（WeakMap やコンテキストキャッシュの利用）。
- **関数のインターフェース設計**:
  - 関数の引数が4つ以上になる場合は、位置引数ではなく必ずオプションオブジェクト（`options: ContextOptions`）にまとめてください。
- **DRY原則とファクトリ関数**:
  - ツリーノードやカード用データなど、巨大なオブジェクトリテラルのコピペ作成を禁止します。必ず共通ファクトリ関数を経由して生成してください。
- **型定義の一元化とデッドコード排除**:
  - 共通型（`MarketItem`, `ListingsDataset`, `CraftCardItem`, `UniversalisListingItem` 等）は `frontend/src/types/index.ts` を通じて一元管理してください。
  - 使われなくなった旧API用のインターフェースや死に型定義を放置せず、定期的にクリーンアップしてください。

---

## 2. フロントエンド：React レンダリング & Canvas 描画

- **無駄な再レンダリングの遮断**:
  - 検索窓の文字入力や数値入力（所持数・数量）の更新が、背後の重たいツリー再計算や無関係なコンポーネントを連鎖再描画させないよう、状態の局所化（State Colocation）と `useMemo` / `useCallback` の依存配列の純化を徹底してください。
  - リスト項目やカンバンカードなど、高頻度に更新される要素は `React.memo` を適用し、差分更新できるようにコンポーネントを適切に分離してください。
- **Canvas ライフサイクルの健全性**:
  - 高DPI（Retina）ディスプレイ環境でチャートや線がぼやけないよう、バッファ解像度とCSS表示サイズを `window.devicePixelRatio` で適切にスケーリングしてください。
  - アニメーションやリサイズ追従は `requestAnimationFrame` および `ResizeObserver` を使用し、コンポーネント破棄時のクリーンアップ（`cancelAnimationFrame` / `disconnect()`）を漏らさず記述してください。
- **ビジネスロジックの分離**:
  - 複雑な計算・需要集計・トポロジカルソートなどの純粋ロジックを JSX コンポーネント内にインラインで記述することを禁止します。必ず `*Utils.ts` 等の純粋関数モジュールに切り出してください。
- **デザインシステムとトンマナの維持**:
  - 既存のダークサイバー調（`#0b0f19`, グラスモーフィズム, グラデーション, Outfit / Inter フォント）の配色・世界観を100%維持してください。

---

## 3. フロントエンド：通信・状態管理・ライフサイクル

- **インフライトリクエストの重複排除（Deduplication）**:
  - 同一アイテム・同一エンドポイントに対する同時期のリクエストは、実行中の `Promise` に合流させ、多重送信による 429 (Rate Limit) の誘発を防止してください。
- **タイムアウトと安全策**:
  - すべての外部通信（Universalis / 静的JSON）には `AbortController` によるタイムアウト（10〜15秒）を設定してください。
  - アプリ起動時のデータ取得は `Promise.allSettled` による並列フェッチを行い、取得失敗時はユーザーが復旧できる再試行UI（フォールバック）を担保してください。モジュール最上位スコープでの通信副作用は厳禁です。
- **キャッシュ無効化（Invalidation）の明示**:
  - インメモリキャッシュには強制更新手段（`invalidateMarketDataCache()` 等）を用意し、ユーザーが手動でデータを最新化できる経路を確保してください。

---

## 4. バックエンド：Python / FastAPI / SQLite / パイプライン

- **SQLite 接続とロック耐性**:
  - すべての接続で `PRAGMA busy_timeout = 10000;`（10秒待機）および WAL モードを設定してください。
  - コネクションは `with conn:` または `try...finally` で確実にクローズし、例外発生時のロールバックを徹底してください。
- **外部API通信の耐障害性**:
  - チャンク取得処理には二分探索救済（バイナリスプリット）を導入し、一部の不良データによるチャンク全体の道連れ破棄を防止してください。
  - HTTP 429（Rate Limit）検出時は、指数バックオフ（Exponential Backoff）による自動リトライを適用してください。
- **メモリ効率とクエリ最適化**:
  - 数十万件規模のレコード処理で巨大なリストをオンメモリに抱え込まず、ジェネレータ（ストリーミング供給）を活用してください。
  - ループ内での個別クエリ発行（N+1問題）を禁止し、`in_()` 等を用いた一括バルク取得を行ってください。
- **定数・定義の一元管理**:
  - DC・ワールド名・URL・タイムアウト値などのハードコードを禁止します。定数は必ず `constants.py` に集約してください。
  - 裸の `except:` や `from module import *`（ワイルドカードインポート）は禁止し、捕捉する例外型やインポートシンボルを明示してください。
- **FastAPI ライフサイクルとセキュリティ**:
  - 起動時・終了時のリソース管理には `@asynccontextmanager` による `lifespan` を利用し、DB 接続確認と `engine.dispose()` を確実に行ってください。
  - CORS 設定で `allow_origins=["*"]` を指定する場合は、W3C / Fetch 仕様違反を防ぐため `allow_credentials=False` を設定してください。

---

## 5. CI/CD & DevOps（GitHub Actions）

- **多重実行の排他制御**:
  - データ更新を伴うパイプラインワークフローは `concurrency.group: ff14-data-pipeline` を指定し、Release アセットや DB キャッシュの並行書き込み衝突を防止してください。
- **キャッシュとタイムアウト**:
  - 依存パッケージ（pip, npm）のキャッシュ設定（`cache: 'pip'`, `cache: 'npm'`）を維持し、すべてのジョブに `timeout-minutes` を設定してハングアップによる実行枠浪費を防止してください。
- **Git 履歴の肥大化防止（Git Hygiene）**:
  - パイプラインで生成される巨大バイナリ（`data.json.gz`, `listings.json.gz`, SQLite DB）はリポジトリ（`.git`）にコミットせず、GitHub Release（`data-latest`）経由で配信してください。
  - Release へのアップロード時は、未生成ファイルによるステップ全体の異常終了を防ぐため、存在するファイルのみを動的に検出してアップロードしてください。

---

## 6. 実装時の遵守ルール（開発エージェントへの指示）

- **既存機能・表示・型定義の破壊禁止**:
  - 新機能追加時も、既存のUIレイアウト、操作感、データフォーマット（`data.json.gz`, `listings.json.gz` 等）、ビルドコマンドの整合性を100%維持してください。
- **段階的な検証**:
  - コード変更後は必ず TypeScript 型チェック（`npm run build`）および Python 構文・インポートチェックを通過させ、エラー・警告ゼロを確認した上で完了報告を行ってください。
- **規約ファイルの継続的遵守**:
  - 今後このリポジトリで作業を行うエージェントおよび開発者は、本規約ファイル（`DEVELOPMENT_RULES.md`）の内容を常に遵守して実装を進めてください。
